main() {
    var a = 10
    
    // 【陷阱 1：重复声明】
    var a = 20
    
    // 【陷阱 2：未声明先引用 (测试断层抢救)】
    // 这里的 unknown_var 根本不存在，看看编译器会不会崩溃！
    var b = unknown_var + 5
    
    // 【陷阱 3：给未声明的变量赋值】
    bad_var = 100
    
    // 【正常代码：看看编译器在报错后还能不能继续工作？】
    if (a > 0) {
        write(b)
    }
}